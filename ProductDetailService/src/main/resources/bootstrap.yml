---
spring:
  cloud:
    config:
      uri: http://localhost:9001
  application:
    name: ProductService
  sleuth:
    sampler:
      percentage: 0.5
      
server:
  port: ${PORT:${SERVER_PORT:0}} 